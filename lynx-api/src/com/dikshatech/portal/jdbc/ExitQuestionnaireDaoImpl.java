/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.dikshatech.portal.jdbc;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.Collection;
import org.apache.log4j.Logger;
import com.dikshatech.portal.dao.ExitQuestionnaireDao;
import com.dikshatech.portal.dto.ExitQuestionnaire;
import com.dikshatech.portal.dto.ExitQuestionnairePk;
import com.dikshatech.portal.exceptions.ExitQuestionnaireDaoException;

public class ExitQuestionnaireDaoImpl extends AbstractDAO implements ExitQuestionnaireDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	protected static final Logger logger = Logger.getLogger( ExitQuestionnaireDaoImpl.class );

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID, EXIT_ID, REASON, PREVENTABLE_STEPS, NEW_EMP_SITUATION, RESPONSIBILITIES, CONCERNS, RECOMMENDATIONS, REJOINING, WORKING_WITH_US, COMMENTS, SUBMITTED_ON FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID, EXIT_ID, REASON, PREVENTABLE_STEPS, NEW_EMP_SITUATION, RESPONSIBILITIES, CONCERNS, RECOMMENDATIONS, REJOINING, WORKING_WITH_US, COMMENTS, SUBMITTED_ON ) VALUES ( ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID = ?, EXIT_ID = ?, REASON = ?, PREVENTABLE_STEPS = ?, NEW_EMP_SITUATION = ?, RESPONSIBILITIES = ?, CONCERNS = ?, RECOMMENDATIONS = ?, REJOINING = ?, WORKING_WITH_US = ?, COMMENTS = ?, SUBMITTED_ON = ? WHERE ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID = ?";

	/** 
	 * Index of column ID
	 */
	protected static final int COLUMN_ID = 1;

	/** 
	 * Index of column EXIT_ID
	 */
	protected static final int COLUMN_EXIT_ID = 2;

	/** 
	 * Index of column REASON
	 */
	protected static final int COLUMN_REASON = 3;

	/** 
	 * Index of column PREVENTABLE_STEPS
	 */
	protected static final int COLUMN_PREVENTABLE_STEPS = 4;

	/** 
	 * Index of column NEW_EMP_SITUATION
	 */
	protected static final int COLUMN_NEW_EMP_SITUATION = 5;

	/** 
	 * Index of column RESPONSIBILITIES
	 */
	protected static final int COLUMN_RESPONSIBILITIES = 6;

	/** 
	 * Index of column CONCERNS
	 */
	protected static final int COLUMN_CONCERNS = 7;

	/** 
	 * Index of column RECOMMENDATIONS
	 */
	protected static final int COLUMN_RECOMMENDATIONS = 8;

	/** 
	 * Index of column REJOINING
	 */
	protected static final int COLUMN_REJOINING = 9;

	/** 
	 * Index of column WORKING_WITH_US
	 */
	protected static final int COLUMN_WORKING_WITH_US = 10;

	/** 
	 * Index of column COMMENTS
	 */
	protected static final int COLUMN_COMMENTS = 11;

	/** 
	 * Index of column SUBMITTED_ON
	 */
	protected static final int COLUMN_SUBMITTED_ON = 12;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 12;

	/** 
	 * Index of primary-key column ID
	 */
	protected static final int PK_COLUMN_ID = 1;

	/** 
	 * Inserts a new row in the exit_questionnaire table.
	 */
	public ExitQuestionnairePk insert(ExitQuestionnaire dto) throws ExitQuestionnaireDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getId() );
			stmt.setInt( index++, dto.getExitId() );
			stmt.setString( index++, dto.getReason() );
			stmt.setString( index++, dto.getPreventableSteps() );
			stmt.setString( index++, dto.getNewEmpSituation() );
			stmt.setString( index++, dto.getResponsibilities() );
			stmt.setString( index++, dto.getConcerns() );
			stmt.setString( index++, dto.getRecommendations() );
			stmt.setString( index++, dto.getRejoining() );
			stmt.setString( index++, dto.getWorkingWithUs() );
			stmt.setString( index++, dto.getComments() );
			stmt.setTimestamp(index++, dto.getSubmittedOn()==null ? null : new java.sql.Timestamp( dto.getSubmittedOn().getTime() ) );
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_INSERT + " with DTO: " + dto);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ExitQuestionnaireDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the exit_questionnaire table.
	 */
	public void update(ExitQuestionnairePk pk, ExitQuestionnaire dto) throws ExitQuestionnaireDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_UPDATE + " with DTO: " + dto);
			}
		
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getId() );
			stmt.setInt( index++, dto.getExitId() );
			stmt.setString( index++, dto.getReason() );
			stmt.setString( index++, dto.getPreventableSteps() );
			stmt.setString( index++, dto.getNewEmpSituation() );
			stmt.setString( index++, dto.getResponsibilities() );
			stmt.setString( index++, dto.getConcerns() );
			stmt.setString( index++, dto.getRecommendations() );
			stmt.setString( index++, dto.getRejoining() );
			stmt.setString( index++, dto.getWorkingWithUs() );
			stmt.setString( index++, dto.getComments() );
			stmt.setTimestamp(index++, dto.getSubmittedOn()==null ? null : new java.sql.Timestamp( dto.getSubmittedOn().getTime() ) );
			stmt.setInt( 13, pk.getId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ExitQuestionnaireDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the exit_questionnaire table.
	 */
	public void delete(ExitQuestionnairePk pk) throws ExitQuestionnaireDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_DELETE + " with PK: " + pk);
			}
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ExitQuestionnaireDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the exit_questionnaire table that matches the specified primary-key value.
	 */
	public ExitQuestionnaire findByPrimaryKey(ExitQuestionnairePk pk) throws ExitQuestionnaireDaoException
	{
		return findByPrimaryKey( pk.getId() );
	}

	/** 
	 * Returns all rows from the exit_questionnaire table that match the criteria 'ID = :id'.
	 */
	public ExitQuestionnaire findByPrimaryKey(int id) throws ExitQuestionnaireDaoException
	{
		ExitQuestionnaire ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID = ?", new Object[] {  new Integer(id) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the exit_questionnaire table that match the criteria ''.
	 */
	public ExitQuestionnaire[] findAll() throws ExitQuestionnaireDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID", null );
	}

	/** 
	 * Returns all rows from the exit_questionnaire table that match the criteria 'EXIT_ID = :exitId'.
	 */
	public ExitQuestionnaire findWhereExitIdEquals(Integer exitId) throws ExitQuestionnaireDaoException
	{
		ExitQuestionnaire ret[] = findByDynamicSelect( SQL_SELECT + " WHERE EXIT_ID = ?", new Object[] { exitId } );
		return ret.length==0 ? null : ret[0];
	}

	/**
	 * Method 'ExitQuestionnaireDaoImpl'
	 * 
	 */
	public ExitQuestionnaireDaoImpl()
	{
	}

	/**
	 * Method 'ExitQuestionnaireDaoImpl'
	 * 
	 * @param userConn
	 */
	public ExitQuestionnaireDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "EXIT_QUESTIONNAIRE";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ExitQuestionnaire fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ExitQuestionnaire dto = new ExitQuestionnaire();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ExitQuestionnaire[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection<ExitQuestionnaire> resultList = new ArrayList<ExitQuestionnaire>();
		while (rs.next()) {
			ExitQuestionnaire dto = new ExitQuestionnaire();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ExitQuestionnaire ret[] = new ExitQuestionnaire[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ExitQuestionnaire dto, ResultSet rs) throws SQLException
	{
		dto.setId( rs.getInt( COLUMN_ID ) );
		dto.setExitId( rs.getInt( COLUMN_EXIT_ID ) );
		dto.setReason( rs.getString( COLUMN_REASON ) );
		dto.setPreventableSteps( rs.getString( COLUMN_PREVENTABLE_STEPS ) );
		dto.setNewEmpSituation( rs.getString( COLUMN_NEW_EMP_SITUATION ) );
		dto.setResponsibilities( rs.getString( COLUMN_RESPONSIBILITIES ) );
		dto.setConcerns( rs.getString( COLUMN_CONCERNS ) );
		dto.setRecommendations( rs.getString( COLUMN_RECOMMENDATIONS ) );
		dto.setRejoining( rs.getString( COLUMN_REJOINING ) );
		dto.setWorkingWithUs( rs.getString( COLUMN_WORKING_WITH_US ) );
		dto.setComments( rs.getString( COLUMN_COMMENTS ) );
		dto.setSubmittedOn( rs.getTimestamp(COLUMN_SUBMITTED_ON ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ExitQuestionnaire dto)
	{
	}

	/** 
	 * Returns all rows from the exit_questionnaire table that match the specified arbitrary SQL statement
	 */
	public ExitQuestionnaire[] findByDynamicSelect(String sql, Object[] sqlParams) throws ExitQuestionnaireDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ExitQuestionnaireDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the exit_questionnaire table that match the specified arbitrary SQL statement
	 */
	public ExitQuestionnaire[] findByDynamicWhere(String sql, Object[] sqlParams) throws ExitQuestionnaireDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ExitQuestionnaireDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
