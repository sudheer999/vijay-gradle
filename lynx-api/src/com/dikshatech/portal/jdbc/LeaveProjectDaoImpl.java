/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.dikshatech.portal.jdbc;

import com.dikshatech.portal.dao.*;
import com.dikshatech.portal.factory.*;
import com.dikshatech.portal.dto.*;
import com.dikshatech.portal.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import org.apache.log4j.Logger;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class LeaveProjectDaoImpl extends AbstractDAO implements LeaveProjectDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	protected static final Logger logger = Logger.getLogger( LeaveProjectDaoImpl.class );

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID, PROJECT_ID, PROJECT_NAME, CHARGE_CODE, TITLE, LEAVE_MASTER_ID FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID, PROJECT_ID, PROJECT_NAME, CHARGE_CODE, TITLE, LEAVE_MASTER_ID ) VALUES ( ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID = ?, PROJECT_ID = ?, PROJECT_NAME = ?, CHARGE_CODE = ?, TITLE = ?, LEAVE_MASTER_ID = ? WHERE ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID = ?";

	/** 
	 * Index of column ID
	 */
	protected static final int COLUMN_ID = 1;

	/** 
	 * Index of column PROJECT_ID
	 */
	protected static final int COLUMN_PROJECT_ID = 2;

	/** 
	 * Index of column PROJECT_NAME
	 */
	protected static final int COLUMN_PROJECT_NAME = 3;

	/** 
	 * Index of column CHARGE_CODE
	 */
	protected static final int COLUMN_CHARGE_CODE = 4;

	/** 
	 * Index of column TITLE
	 */
	protected static final int COLUMN_TITLE = 5;

	/** 
	 * Index of column LEAVE_MASTER_ID
	 */
	protected static final int COLUMN_LEAVE_MASTER_ID = 6;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 6;

	/** 
	 * Index of primary-key column ID
	 */
	protected static final int PK_COLUMN_ID = 1;

	/** 
	 * Inserts a new row in the LEAVE_PROJECT table.
	 */
	public LeaveProjectPk insert(LeaveProject dto) throws LeaveProjectDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			stmt.setInt( index++, dto.getId() );
			stmt.setString( index++, dto.getProjectId() );
			stmt.setString( index++, dto.getProjectName() );
			stmt.setString( index++, dto.getChargeCode() );
			stmt.setString( index++, dto.getTitle() );
			stmt.setInt( index++, dto.getLeaveMasterId() );
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_INSERT + " with DTO: " + dto);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setId( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new LeaveProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the LEAVE_PROJECT table.
	 */
	public void update(LeaveProjectPk pk, LeaveProject dto) throws LeaveProjectDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_UPDATE + " with DTO: " + dto);
			}
		
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getId() );
			stmt.setString( index++, dto.getProjectId() );
			stmt.setString( index++, dto.getProjectName() );
			stmt.setString( index++, dto.getChargeCode() );
			stmt.setString( index++, dto.getTitle() );
			stmt.setInt( index++, dto.getLeaveMasterId() );
			stmt.setInt( 7, pk.getId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new LeaveProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the LEAVE_PROJECT table.
	 */
	public void delete(LeaveProjectPk pk) throws LeaveProjectDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_DELETE + " with PK: " + pk);
			}
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new LeaveProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the LEAVE_PROJECT table that matches the specified primary-key value.
	 */
	public LeaveProject findByPrimaryKey(LeaveProjectPk pk) throws LeaveProjectDaoException
	{
		return findByPrimaryKey( pk.getId() );
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the criteria 'ID = :id'.
	 */
	public LeaveProject findByPrimaryKey(int id) throws LeaveProjectDaoException
	{
		LeaveProject ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID = ?", new Object[] {  new Integer(id) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the criteria ''.
	 */
	public LeaveProject[] findAll() throws LeaveProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID", null );
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the criteria 'ID = :id'.
	 */
	public LeaveProject[] findWhereIdEquals(int id) throws LeaveProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID = ? ORDER BY ID", new Object[] {  new Integer(id) } );
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the criteria 'PROJECT_ID = :projectId'.
	 */
	public LeaveProject[] findWhereProjectIdEquals(String projectId) throws LeaveProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PROJECT_ID = ? ORDER BY PROJECT_ID", new Object[] { projectId } );
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the criteria 'PROJECT_NAME = :projectName'.
	 */
	public LeaveProject[] findWhereProjectNameEquals(String projectName) throws LeaveProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PROJECT_NAME = ? ORDER BY PROJECT_NAME", new Object[] { projectName } );
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the criteria 'CHARGE_CODE = :chargeCode'.
	 */
	public LeaveProject[] findWhereChargeCodeEquals(String chargeCode) throws LeaveProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CHARGE_CODE = ? ORDER BY CHARGE_CODE", new Object[] { chargeCode } );
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the criteria 'TITLE = :title'.
	 */
	public LeaveProject[] findWhereTitleEquals(String title) throws LeaveProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TITLE = ? ORDER BY TITLE", new Object[] { title } );
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the criteria 'LEAVE_MASTER_ID = :leaveMasterId'.
	 */
	public LeaveProject[] findWhereLeaveMasterIdEquals(int leaveMasterId) throws LeaveProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE LEAVE_MASTER_ID = ? ORDER BY LEAVE_MASTER_ID", new Object[] {  new Integer(leaveMasterId) } );
	}

	/**
	 * Method 'LeaveProjectDaoImpl'
	 * 
	 */
	public LeaveProjectDaoImpl()
	{
	}

	/**
	 * Method 'LeaveProjectDaoImpl'
	 * 
	 * @param userConn
	 */
	public LeaveProjectDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "LEAVE_PROJECT";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected LeaveProject fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			LeaveProject dto = new LeaveProject();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected LeaveProject[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection<LeaveProject> resultList = new ArrayList<LeaveProject>();
		while (rs.next()) {
			LeaveProject dto = new LeaveProject();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		LeaveProject ret[] = new LeaveProject[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(LeaveProject dto, ResultSet rs) throws SQLException
	{
		dto.setId( rs.getInt( COLUMN_ID ) );
		dto.setProjectId( rs.getString( COLUMN_PROJECT_ID ) );
		dto.setProjectName( rs.getString( COLUMN_PROJECT_NAME ) );
		dto.setChargeCode( rs.getString( COLUMN_CHARGE_CODE ) );
		dto.setTitle( rs.getString( COLUMN_TITLE ) );
		dto.setLeaveMasterId( rs.getInt( COLUMN_LEAVE_MASTER_ID ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(LeaveProject dto)
	{
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the specified arbitrary SQL statement
	 */
	public LeaveProject[] findByDynamicSelect(String sql, Object[] sqlParams) throws LeaveProjectDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new LeaveProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the LEAVE_PROJECT table that match the specified arbitrary SQL statement
	 */
	public LeaveProject[] findByDynamicWhere(String sql, Object[] sqlParams) throws LeaveProjectDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new LeaveProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
