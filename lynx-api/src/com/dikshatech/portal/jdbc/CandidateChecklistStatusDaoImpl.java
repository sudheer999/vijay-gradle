/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.dikshatech.portal.jdbc;

import com.dikshatech.portal.dao.*;
import com.dikshatech.portal.factory.*;
import com.dikshatech.portal.dto.*;
import com.dikshatech.portal.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import org.apache.log4j.Logger;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class CandidateChecklistStatusDaoImpl extends AbstractDAO implements CandidateChecklistStatusDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	protected static final Logger logger = Logger.getLogger( CandidateChecklistStatusDaoImpl.class );

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID, CAND_ID, DOC_ID, IS_UPLOADED, STATUS, COMMENTS, CHKLST_DOC_ID FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID, CAND_ID, DOC_ID, IS_UPLOADED, STATUS, COMMENTS, CHKLST_DOC_ID ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID = ?, CAND_ID = ?, DOC_ID = ?, IS_UPLOADED = ?, STATUS = ?, COMMENTS = ?, CHKLST_DOC_ID = ? WHERE ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID = ?";

	/** 
	 * Index of column ID
	 */
	protected static final int COLUMN_ID = 1;

	/** 
	 * Index of column CAND_ID
	 */
	protected static final int COLUMN_CAND_ID = 2;

	/** 
	 * Index of column DOC_ID
	 */
	protected static final int COLUMN_DOC_ID = 3;

	/** 
	 * Index of column IS_UPLOADED
	 */
	protected static final int COLUMN_IS_UPLOADED = 4;

	/** 
	 * Index of column STATUS
	 */
	protected static final int COLUMN_STATUS = 5;

	/** 
	 * Index of column COMMENTS
	 */
	protected static final int COLUMN_COMMENTS = 6;

	/** 
	 * Index of column CHKLST_DOC_ID
	 */
	protected static final int COLUMN_CHKLST_DOC_ID = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Index of primary-key column ID
	 */
	protected static final int PK_COLUMN_ID = 1;

	/** 
	 * Inserts a new row in the CANDIDATE_CHECKLIST_STATUS table.
	 */
	public CandidateChecklistStatusPk insert(CandidateChecklistStatus dto) throws CandidateChecklistStatusDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			stmt.setInt( index++, dto.getId() );
			stmt.setInt( index++, dto.getCandId() );
			stmt.setInt( index++, dto.getDocId() );
			stmt.setInt( index++, dto.getIsUploaded() );
			stmt.setString( index++, dto.getStatus() );
			stmt.setString( index++, dto.getComments() );
			stmt.setInt( index++, dto.getChklstDocId() );
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_INSERT + " with DTO: " + dto);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setId( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new CandidateChecklistStatusDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the CANDIDATE_CHECKLIST_STATUS table.
	 */
	public void update(CandidateChecklistStatusPk pk, CandidateChecklistStatus dto) throws CandidateChecklistStatusDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_UPDATE + " with DTO: " + dto);
			}
		
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getId() );
			stmt.setInt( index++, dto.getCandId() );
			stmt.setInt( index++, dto.getDocId() );
			stmt.setInt( index++, dto.getIsUploaded() );
			stmt.setString( index++, dto.getStatus() );
			stmt.setString( index++, dto.getComments() );
			stmt.setInt( index++, dto.getChklstDocId() );
			stmt.setInt( 8, pk.getId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new CandidateChecklistStatusDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the CANDIDATE_CHECKLIST_STATUS table.
	 */
	public void delete(CandidateChecklistStatusPk pk) throws CandidateChecklistStatusDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_DELETE + " with PK: " + pk);
			}
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new CandidateChecklistStatusDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the CANDIDATE_CHECKLIST_STATUS table that matches the specified primary-key value.
	 */
	public CandidateChecklistStatus findByPrimaryKey(CandidateChecklistStatusPk pk) throws CandidateChecklistStatusDaoException
	{
		return findByPrimaryKey( pk.getId() );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'ID = :id'.
	 */
	public CandidateChecklistStatus findByPrimaryKey(int id) throws CandidateChecklistStatusDaoException
	{
		CandidateChecklistStatus ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID = ?", new Object[] {  new Integer(id) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'CAND_DOC_ID = :candDocId'.
	 */
	public CandidateChecklistStatus[] findWhereCandDocIdEquals(int candDocId) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CAND_DOC_ID = ? ORDER BY CAND_DOC_ID", new Object[] {  new Integer(candDocId) } );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria ''.
	 */
	public CandidateChecklistStatus[] findAll() throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID", null );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'CHKLST_DOC_ID = :chklstDocId'.
	 */
	public CandidateChecklistStatus[] findByCandidateChecklistDocs(int chklstDocId) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CHKLST_DOC_ID = ?", new Object[] {  new Integer(chklstDocId) } );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'ID = :id'.
	 */
	public CandidateChecklistStatus[] findWhereIdEquals(int id) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID = ? ORDER BY ID", new Object[] {  new Integer(id) } );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'CAND_ID = :candId'.
	 */
	public CandidateChecklistStatus[] findWhereCandIdEquals(int candId) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CAND_ID = ? ORDER BY CAND_ID", new Object[] {  new Integer(candId) } );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'CHKLST_DOC_ID = :chklstDocId'.
	 */
	public CandidateChecklistStatus[] findWhereChklstDocIdEquals(int chklstDocId) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CHKLST_DOC_ID = ? ORDER BY CHKLST_DOC_ID", new Object[] {  new Integer(chklstDocId) } );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'DOC_ID = :docId'.
	 */
	public CandidateChecklistStatus[] findWhereDocIdEquals(int docId) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DOC_ID = ? ORDER BY DOC_ID", new Object[] {  new Integer(docId) } );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'IS_UPLOADED = :isUploaded'.
	 */
	public CandidateChecklistStatus[] findWhereIsUploadedEquals(int isUploaded) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IS_UPLOADED = ? ORDER BY IS_UPLOADED", new Object[] {  new Integer(isUploaded) } );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'STATUS = :status'.
	 */
	public CandidateChecklistStatus[] findWhereStatusEquals(String status) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE STATUS = ? ORDER BY STATUS", new Object[] { status } );
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the criteria 'COMMENTS = :comments'.
	 */
	public CandidateChecklistStatus[] findWhereCommentsEquals(String comments) throws CandidateChecklistStatusDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE COMMENTS = ? ORDER BY COMMENTS", new Object[] { comments } );
	}

	/**
	 * Method 'CandidateChecklistStatusDaoImpl'
	 * 
	 */
	public CandidateChecklistStatusDaoImpl()
	{
	}

	/**
	 * Method 'CandidateChecklistStatusDaoImpl'
	 * 
	 * @param userConn
	 */
	public CandidateChecklistStatusDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "CANDIDATE_CHECKLIST_STATUS";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected CandidateChecklistStatus fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			CandidateChecklistStatus dto = new CandidateChecklistStatus();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected CandidateChecklistStatus[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection<CandidateChecklistStatus> resultList = new ArrayList<CandidateChecklistStatus>();
		while (rs.next()) {
			CandidateChecklistStatus dto = new CandidateChecklistStatus();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		CandidateChecklistStatus ret[] = new CandidateChecklistStatus[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(CandidateChecklistStatus dto, ResultSet rs) throws SQLException
	{
		dto.setId( rs.getInt( COLUMN_ID ) );
		dto.setCandId( rs.getInt( COLUMN_CAND_ID ) );
		dto.setDocId( rs.getInt( COLUMN_DOC_ID ) );
		dto.setIsUploaded( rs.getInt( COLUMN_IS_UPLOADED ) );
		dto.setStatus( rs.getString( COLUMN_STATUS ) );
		dto.setComments( rs.getString( COLUMN_COMMENTS ) );
		dto.setChklstDocId( rs.getInt( COLUMN_CHKLST_DOC_ID ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(CandidateChecklistStatus dto)
	{
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the specified arbitrary SQL statement
	 */
	public CandidateChecklistStatus[] findByDynamicSelect(String sql, Object[] sqlParams) throws CandidateChecklistStatusDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new CandidateChecklistStatusDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the CANDIDATE_CHECKLIST_STATUS table that match the specified arbitrary SQL statement
	 */
	public CandidateChecklistStatus[] findByDynamicWhere(String sql, Object[] sqlParams) throws CandidateChecklistStatusDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new CandidateChecklistStatusDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
