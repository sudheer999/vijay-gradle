/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.dikshatech.portal.jdbc;

import com.dikshatech.portal.dao.*;
import com.dikshatech.portal.factory.*;
import com.dikshatech.portal.dto.*;
import com.dikshatech.portal.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import org.apache.log4j.Logger;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class AddressDaoImpl extends AbstractDAO implements AddressDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	protected static final Logger logger = Logger.getLogger( AddressDaoImpl.class );

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID, ADDRESS, CITY, ZIPCODE, STATE, COUNTRY FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID, ADDRESS, CITY, ZIPCODE, STATE, COUNTRY ) VALUES ( ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID = ?, ADDRESS = ?, CITY = ?, ZIPCODE = ?, STATE = ?, COUNTRY = ? WHERE ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID = ?";

	/** 
	 * Index of column ID
	 */
	protected static final int COLUMN_ID = 1;

	/** 
	 * Index of column ADDRESS
	 */
	protected static final int COLUMN_ADDRESS = 2;

	/** 
	 * Index of column CITY
	 */
	protected static final int COLUMN_CITY = 3;

	/** 
	 * Index of column ZIPCODE
	 */
	protected static final int COLUMN_ZIPCODE = 4;

	/** 
	 * Index of column STATE
	 */
	protected static final int COLUMN_STATE = 5;

	/** 
	 * Index of column COUNTRY
	 */
	protected static final int COLUMN_COUNTRY = 6;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 6;

	/** 
	 * Index of primary-key column ID
	 */
	protected static final int PK_COLUMN_ID = 1;

	/** 
	 * Inserts a new row in the ADDRESS table.
	 */
	public AddressPk insert(Address dto) throws AddressDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			stmt.setInt( index++, dto.getId() );
			stmt.setString( index++, dto.getAddress() );
			stmt.setString( index++, dto.getCity() );
			if (dto.isZipcodeNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getZipcode() );
			}
		
			stmt.setString( index++, dto.getState() );
			stmt.setString( index++, dto.getCountry() );
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_INSERT + " with DTO: " + dto);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setId( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new AddressDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the ADDRESS table.
	 */
	public void update(AddressPk pk, Address dto) throws AddressDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_UPDATE + " with DTO: " + dto);
			}
		
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getId() );
			stmt.setString( index++, dto.getAddress() );
			stmt.setString( index++, dto.getCity() );
			if (dto.isZipcodeNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getZipcode() );
			}
		
			stmt.setString( index++, dto.getState() );
			stmt.setString( index++, dto.getCountry() );
			stmt.setInt( 7, pk.getId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new AddressDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the ADDRESS table.
	 */
	public void delete(AddressPk pk) throws AddressDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_DELETE + " with PK: " + pk);
			}
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new AddressDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the ADDRESS table that matches the specified primary-key value.
	 */
	public Address findByPrimaryKey(AddressPk pk) throws AddressDaoException
	{
		return findByPrimaryKey( pk.getId() );
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the criteria 'ID = :id'.
	 */
	public Address findByPrimaryKey(int id) throws AddressDaoException
	{
		Address ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID = ?", new Object[] {  new Integer(id) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the criteria ''.
	 */
	public Address[] findAll() throws AddressDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID", null );
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the criteria 'ID = :id'.
	 */
	public Address[] findWhereIdEquals(int id) throws AddressDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID = ? ORDER BY ID", new Object[] {  new Integer(id) } );
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the criteria 'ADDRESS = :address'.
	 */
	public Address[] findWhereAddressEquals(String address) throws AddressDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ADDRESS = ? ORDER BY ADDRESS", new Object[] { address } );
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the criteria 'CITY = :city'.
	 */
	public Address[] findWhereCityEquals(String city) throws AddressDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CITY = ? ORDER BY CITY", new Object[] { city } );
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the criteria 'ZIPCODE = :zipcode'.
	 */
	public Address[] findWhereZipcodeEquals(int zipcode) throws AddressDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ZIPCODE = ? ORDER BY ZIPCODE", new Object[] {  new Integer(zipcode) } );
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the criteria 'STATE = :state'.
	 */
	public Address[] findWhereStateEquals(String state) throws AddressDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE STATE = ? ORDER BY STATE", new Object[] { state } );
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the criteria 'COUNTRY = :country'.
	 */
	public Address[] findWhereCountryEquals(String country) throws AddressDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE COUNTRY = ? ORDER BY COUNTRY", new Object[] { country } );
	}

	/**
	 * Method 'AddressDaoImpl'
	 * 
	 */
	public AddressDaoImpl()
	{
	}

	/**
	 * Method 'AddressDaoImpl'
	 * 
	 * @param userConn
	 */
	public AddressDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "ADDRESS";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Address fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Address dto = new Address();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Address[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection<Address> resultList = new ArrayList<Address>();
		while (rs.next()) {
			Address dto = new Address();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Address ret[] = new Address[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Address dto, ResultSet rs) throws SQLException
	{
		dto.setId( rs.getInt( COLUMN_ID ) );
		dto.setAddress( rs.getString( COLUMN_ADDRESS ) );
		dto.setCity( rs.getString( COLUMN_CITY ) );
		dto.setZipcode( rs.getInt( COLUMN_ZIPCODE ) );
		if (rs.wasNull()) {
			dto.setZipcodeNull( true );
		}
		
		dto.setState( rs.getString( COLUMN_STATE ) );
		dto.setCountry( rs.getString( COLUMN_COUNTRY ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Address dto)
	{
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the specified arbitrary SQL statement
	 */
	public Address[] findByDynamicSelect(String sql, Object[] sqlParams) throws AddressDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new AddressDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the ADDRESS table that match the specified arbitrary SQL statement
	 */
	public Address[] findByDynamicWhere(String sql, Object[] sqlParams) throws AddressDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new AddressDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
