/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.dikshatech.portal.jdbc;

import com.dikshatech.portal.dao.*;
import com.dikshatech.portal.factory.*;
import com.dikshatech.portal.dto.*;
import com.dikshatech.portal.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import org.apache.log4j.Logger;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EscalationDaoImpl extends AbstractDAO implements EscalationDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	protected static final Logger logger = Logger.getLogger( EscalationDaoImpl.class );

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID, PC_ID, DUE_DAYS, LEVEL_ID, USERS_ID, ESCAL_TO, ESCAL_TYPE FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID, PC_ID, DUE_DAYS, LEVEL_ID, USERS_ID, ESCAL_TO, ESCAL_TYPE ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID = ?, PC_ID = ?, DUE_DAYS = ?, LEVEL_ID = ?, USERS_ID = ?, ESCAL_TO = ?, ESCAL_TYPE = ? WHERE ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID = ?";

	/** 
	 * Index of column ID
	 */
	protected static final int COLUMN_ID = 1;

	/** 
	 * Index of column PC_ID
	 */
	protected static final int COLUMN_PC_ID = 2;

	/** 
	 * Index of column DUE_DAYS
	 */
	protected static final int COLUMN_DUE_DAYS = 3;

	/** 
	 * Index of column LEVEL_ID
	 */
	protected static final int COLUMN_LEVEL_ID = 4;

	/** 
	 * Index of column USERS_ID
	 */
	protected static final int COLUMN_USERS_ID = 5;

	/** 
	 * Index of column ESCAL_TO
	 */
	protected static final int COLUMN_ESCAL_TO = 6;

	/** 
	 * Index of column ESCAL_TYPE
	 */
	protected static final int COLUMN_ESCAL_TYPE = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Index of primary-key column ID
	 */
	protected static final int PK_COLUMN_ID = 1;

	/** 
	 * Inserts a new row in the ESCALATION table.
	 */
	public EscalationPk insert(Escalation dto) throws EscalationDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			stmt.setInt( index++, dto.getId() );
			stmt.setInt( index++, dto.getPcId() );
			if (dto.isDueDaysNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getDueDays() );
			}
		
			if (dto.isLevelIdNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getLevelId() );
			}
		
			stmt.setString( index++, dto.getUsersId() );
			stmt.setString( index++, dto.getEscalTo() );
			stmt.setString( index++, dto.getEscalType() );
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_INSERT + " with DTO: " + dto);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setId( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new EscalationDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the ESCALATION table.
	 */
	public void update(EscalationPk pk, Escalation dto) throws EscalationDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_UPDATE + " with DTO: " + dto);
			}
		
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getId() );
			stmt.setInt( index++, dto.getPcId() );
			if (dto.isDueDaysNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getDueDays() );
			}
		
			if (dto.isLevelIdNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getLevelId() );
			}
		
			stmt.setString( index++, dto.getUsersId() );
			stmt.setString( index++, dto.getEscalTo() );
			stmt.setString( index++, dto.getEscalType() );
			stmt.setInt( 8, pk.getId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new EscalationDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the ESCALATION table.
	 */
	public void delete(EscalationPk pk) throws EscalationDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_DELETE + " with PK: " + pk);
			}
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new EscalationDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the ESCALATION table that matches the specified primary-key value.
	 */
	public Escalation findByPrimaryKey(EscalationPk pk) throws EscalationDaoException
	{
		return findByPrimaryKey( pk.getId() );
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria 'ID = :id'.
	 */
	public Escalation findByPrimaryKey(int id) throws EscalationDaoException
	{
		Escalation ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID = ?", new Object[] {  new Integer(id) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria ''.
	 */
	public Escalation[] findAll() throws EscalationDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID", null );
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria 'ID = :id'.
	 */
	public Escalation[] findWhereIdEquals(int id) throws EscalationDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID = ? ORDER BY ID", new Object[] {  new Integer(id) } );
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria 'PC_ID = :pcId'.
	 */
	public Escalation[] findWherePcIdEquals(int pcId) throws EscalationDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PC_ID = ? ORDER BY PC_ID", new Object[] {  new Integer(pcId) } );
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria 'DUE_DAYS = :dueDays'.
	 */
	public Escalation[] findWhereDueDaysEquals(int dueDays) throws EscalationDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DUE_DAYS = ? ORDER BY DUE_DAYS", new Object[] {  new Integer(dueDays) } );
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria 'LEVEL_ID = :levelId'.
	 */
	public Escalation[] findWhereLevelIdEquals(int levelId) throws EscalationDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE LEVEL_ID = ? ORDER BY LEVEL_ID", new Object[] {  new Integer(levelId) } );
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria 'USERS_ID = :usersId'.
	 */
	public Escalation[] findWhereUsersIdEquals(String usersId) throws EscalationDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE USERS_ID = ? ORDER BY USERS_ID", new Object[] { usersId } );
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria 'ESCAL_TO = :escalTo'.
	 */
	public Escalation[] findWhereEscalToEquals(String escalTo) throws EscalationDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ESCAL_TO = ? ORDER BY ESCAL_TO", new Object[] { escalTo } );
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the criteria 'ESCAL_TYPE = :escalType'.
	 */
	public Escalation[] findWhereEscalTypeEquals(String escalType) throws EscalationDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ESCAL_TYPE = ? ORDER BY ESCAL_TYPE", new Object[] { escalType } );
	}

	/**
	 * Method 'EscalationDaoImpl'
	 * 
	 */
	public EscalationDaoImpl()
	{
	}

	/**
	 * Method 'EscalationDaoImpl'
	 * 
	 * @param userConn
	 */
	public EscalationDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "ESCALATION";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Escalation fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Escalation dto = new Escalation();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Escalation[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection<Escalation> resultList = new ArrayList<Escalation>();
		while (rs.next()) {
			Escalation dto = new Escalation();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Escalation ret[] = new Escalation[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Escalation dto, ResultSet rs) throws SQLException
	{
		dto.setId( rs.getInt( COLUMN_ID ) );
		dto.setPcId( rs.getInt( COLUMN_PC_ID ) );
		dto.setDueDays( rs.getInt( COLUMN_DUE_DAYS ) );
		if (rs.wasNull()) {
			dto.setDueDaysNull( true );
		}
		
		dto.setLevelId( rs.getInt( COLUMN_LEVEL_ID ) );
		if (rs.wasNull()) {
			dto.setLevelIdNull( true );
		}
		
		dto.setUsersId( rs.getString( COLUMN_USERS_ID ) );
		dto.setEscalTo( rs.getString( COLUMN_ESCAL_TO ) );
		dto.setEscalType( rs.getString( COLUMN_ESCAL_TYPE ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Escalation dto)
	{
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the specified arbitrary SQL statement
	 */
	public Escalation[] findByDynamicSelect(String sql, Object[] sqlParams) throws EscalationDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new EscalationDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the ESCALATION table that match the specified arbitrary SQL statement
	 */
	public Escalation[] findByDynamicWhere(String sql, Object[] sqlParams) throws EscalationDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new EscalationDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
