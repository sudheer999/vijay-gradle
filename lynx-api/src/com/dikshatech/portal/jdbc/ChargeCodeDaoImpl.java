/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.dikshatech.portal.jdbc;

import com.dikshatech.portal.dao.*;
import com.dikshatech.portal.factory.*;
import com.dikshatech.portal.dto.*;
import com.dikshatech.portal.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import org.apache.log4j.Logger;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ChargeCodeDaoImpl extends AbstractDAO implements ChargeCodeDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	protected static final Logger logger = Logger.getLogger( ChargeCodeDaoImpl.class );

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID, PO_ID, CH_CODE_NAME, CH_CODE, AUTH_USERS FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID, PO_ID, CH_CODE_NAME, CH_CODE, AUTH_USERS ) VALUES ( ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID = ?, PO_ID = ?, CH_CODE_NAME = ?, CH_CODE = ?, AUTH_USERS = ? WHERE ID = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID = ?";

	/** 
	 * Index of column ID
	 */
	protected static final int COLUMN_ID = 1;

	/** 
	 * Index of column PO_ID
	 */
	protected static final int COLUMN_PO_ID = 2;

	/** 
	 * Index of column CH_CODE_NAME
	 */
	protected static final int COLUMN_CH_CODE_NAME = 3;

	/** 
	 * Index of column CH_CODE
	 */
	protected static final int COLUMN_CH_CODE = 4;

	/** 
	 * Index of column AUTH_USERS
	 */
	protected static final int COLUMN_AUTH_USERS = 5;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 5;

	/** 
	 * Index of primary-key column ID
	 */
	protected static final int PK_COLUMN_ID = 1;

	/** 
	 * Inserts a new row in the CHARGE_CODE table.
	 */
	public ChargeCodePk insert(ChargeCode dto) throws ChargeCodeDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.getId() != null) {
				stmt.setInt( index++, dto.getId().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			if (dto.getPoId() != null) {
				stmt.setInt( index++, dto.getPoId().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			stmt.setString( index++, dto.getChCodeName() );
			stmt.setString( index++, dto.getChCode() );
			stmt.setString( index++, dto.getAuthUsers() );
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_INSERT + " with DTO: " + dto);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setId( new Integer( rs.getInt(1) ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ChargeCodeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the CHARGE_CODE table.
	 */
	public void update(ChargeCodePk pk, ChargeCode dto) throws ChargeCodeDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_UPDATE + " with DTO: " + dto);
			}
		
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			if (dto.getId() != null) {
				stmt.setInt( index++, dto.getId().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			if (dto.getPoId() != null) {
				stmt.setInt( index++, dto.getPoId().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			stmt.setString( index++, dto.getChCodeName() );
			stmt.setString( index++, dto.getChCode() );
			stmt.setString( index++, dto.getAuthUsers() );
			stmt.setInt( 6, pk.getId() );
					
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ChargeCodeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the CHARGE_CODE table.
	 */
	public void delete(ChargeCodePk pk) throws ChargeCodeDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_DELETE + " with PK: " + pk);
			}
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getId() );
			
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ChargeCodeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the CHARGE_CODE table that matches the specified primary-key value.
	 */
	public ChargeCode findByPrimaryKey(ChargeCodePk pk) throws ChargeCodeDaoException
	{
		return findByPrimaryKey( pk.getId() );
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the criteria 'ID = :id'.
	 */
	public ChargeCode findByPrimaryKey(Integer id) throws ChargeCodeDaoException
	{
		ChargeCode ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID = ?", new Object[] { id } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the criteria ''.
	 */
	public ChargeCode[] findAll() throws ChargeCodeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID", null );
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the criteria 'PO_ID = :poId'.
	 */
	public ChargeCode[] findByPoDetails(Integer poId) throws ChargeCodeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PO_ID = ?", new Object[] { poId } );
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the criteria 'ID = :id'.
	 */
	public ChargeCode[] findWhereIdEquals(Integer id) throws ChargeCodeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID = ? ORDER BY ID", new Object[] { id } );
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the criteria 'PO_ID = :poId'.
	 */
	public ChargeCode[] findWherePoIdEquals(Integer poId) throws ChargeCodeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PO_ID = ? ORDER BY PO_ID", new Object[] { poId } );
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the criteria 'CH_CODE_NAME = :chCodeName'.
	 */
	public ChargeCode[] findWhereChCodeNameEquals(String chCodeName) throws ChargeCodeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CH_CODE_NAME = ? ORDER BY CH_CODE_NAME", new Object[] { chCodeName } );
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the criteria 'CH_CODE = :chCode'.
	 */
	public ChargeCode[] findWhereChCodeEquals(String chCode) throws ChargeCodeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CH_CODE = ? ORDER BY CH_CODE", new Object[] { chCode } );
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the criteria 'AUTH_USERS = :authUsers'.
	 */
	public ChargeCode[] findWhereAuthUsersEquals(String authUsers) throws ChargeCodeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE AUTH_USERS = ? ORDER BY AUTH_USERS", new Object[] { authUsers } );
	}

	/**
	 * Method 'ChargeCodeDaoImpl'
	 * 
	 */
	public ChargeCodeDaoImpl()
	{
	}

	/**
	 * Method 'ChargeCodeDaoImpl'
	 * 
	 * @param userConn
	 */
	public ChargeCodeDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "CHARGE_CODE";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ChargeCode fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ChargeCode dto = new ChargeCode();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ChargeCode[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection<ChargeCode> resultList = new ArrayList<ChargeCode>();
		while (rs.next()) {
			ChargeCode dto = new ChargeCode();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ChargeCode ret[] = new ChargeCode[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ChargeCode dto, ResultSet rs) throws SQLException
	{
		dto.setId( new Integer( rs.getInt(COLUMN_ID) ) );
		dto.setPoId( new Integer( rs.getInt(COLUMN_PO_ID) ) );
		dto.setChCodeName( rs.getString( COLUMN_CH_CODE_NAME ) );
		dto.setChCode( rs.getString( COLUMN_CH_CODE ) );
		dto.setAuthUsers( rs.getString( COLUMN_AUTH_USERS ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ChargeCode dto)
	{
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the specified arbitrary SQL statement
	 */
	public ChargeCode[] findByDynamicSelect(String sql, Object[] sqlParams) throws ChargeCodeDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ChargeCodeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the CHARGE_CODE table that match the specified arbitrary SQL statement
	 */
	public ChargeCode[] findByDynamicWhere(String sql, Object[] sqlParams) throws ChargeCodeDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ChargeCodeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}